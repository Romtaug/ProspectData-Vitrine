# ProspectData - Netlify publie la racine du depot telle quelle.
# Les fonctions serveur (chatbot Gemini) vivent dans netlify/functions.

[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
